// p4_4.c
#include <stdio.h>

int main(void)
{
int age = 20;  // 测试数据
char score = 'A';  // 测试数据

if (age > 18)
if(score == 'A')
printf("恭喜，获得青少年组一等奖！");
else
printf("抱歉，本活动只限年龄小于18岁的青少年参与。");

    return 0;
}
